/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(nameFriends?: string): string {

  return nameFriends ? "One for " + nameFriends + ", one for me." : "One for you, one for me."

}
